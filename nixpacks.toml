[phases.setup]
nixPkgs = ["nodejs_22", "pnpm-9_x"]

[phases.install]
cmds = ["corepack enable", "pnpm install --frozen-lockfile=false"]

[phases.build]
cmds = [
  "pnpm --filter @kws/shared build",
  "pnpm --filter @kws/api build"
]

[start]
cmd = "node apps/api/dist/index.js"
